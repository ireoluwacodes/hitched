# Convex (auto-generated by `bunx convex dev` — local only)
VITE_CONVEX_URL=

# Vercel production/preview builds: set CONVEX_DEPLOY_KEY in Vercel (not here).
# Production key → Production env only. Preview key → Preview env only.
# Generate keys in Convex Dashboard → Project Settings → Deploy Keys.
# CONVEX_DEPLOY_KEY=

# Production URL encoded in table QR codes (set in Vercel for deploys).
# For local phone testing on your LAN, use Vite's Network URL, e.g.:
# VITE_APP_URL=http://192.168.1.124:5173
VITE_APP_URL=
